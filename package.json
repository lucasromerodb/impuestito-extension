{
  "scripts": {
    "build:chromium": "cp manifest-chromium.json extension/manifest.json && node compress-extension.js chromium",
    "build:firefox": "cp manifest-firefox.json extension/manifest.json && node compress-extension.js firefox",
    "pack": "node scripts/compress-extension.js",
    "bump": "node scripts/bump-manifest-version.js",
    "test": "jest --watch"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "babel-jest": "^29.7.0",
    "bestzip": "^2.2.1",
    "jest": "^29.7.0"
  },
  "jest": {
    "transform": {
      "^.+\\.js$": "babel-jest"
    }
  }
}
